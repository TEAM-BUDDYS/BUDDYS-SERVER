global:
  scrape_interval: 30s

scrape_configs:
  - job_name: "buddys-server"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8081"]
        labels:
          color: blue
      - targets: ["host.docker.internal:8082"]
        labels:
          color: green
